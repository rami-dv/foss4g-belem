import MdxLayout from "@/components/mdx-layout";
import Heading from "@/components/heading";

import BeiraRio from "./venue/_beira-rio.mdx";
import Hangar from "./venue/_hangar.mdx";
import Ifpa from "./venue/_ifpa.mdx";

import Hangar1 from "@/images/hangar/hangar1.jpg";

export const metadata = {
  title: "Venue",
  description: "Venue information for the events at the FOSS4G 2024 conference in Belém Brasil.",
  image: Hangar1
};

<Heading bubble>Conference Venue</Heading>

<Hangar />
<Ifpa />
<BeiraRio />

export default function MDXPage({ children }) {
  return <MdxLayout metadata={metadata}>{children}</MdxLayout>

}

export async function getStaticProps() {
  return {
    props: {
      metadata
    },
  }
}