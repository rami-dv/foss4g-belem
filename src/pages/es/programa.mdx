import MdxLayout from "@/components/MDXLayout";
import WeekSchedule from "@/images/week-schedule.png";
import ImportantDates from "@/images/important-dates.png";
import Image from "next/image";

export const metadata = {
  title: "Programación",
  description: "Programación general de la Conferencia FOSS4G 2024 celebrada en Belém, Brasil del 02 al 08 de diciembre de 2024",
  image: WeekSchedule, ImportantDates
};

<div className="text-3xl mt-4 mb-2 md:text-7xl text-center md:mt-8 md:mb-4 font-ubuntu">02 al 08 de diciembre de 2024</div>

<Image placeholder="blur" src={WeekSchedule} />

<div className="text-3xl mt-4 mb-2 md:text-7xl text-center md:mt-8 md:mb-4 font-ubuntu"></div>

<Image placeholder="blur" src={ImportantDates} />

export default function MDXPage({ children }) {
  return <MdxLayout metadata={metadata}>{children}</MdxLayout>


}

export async function getStaticProps() {
  return {
    props: {
      metadata
    },
  }
}
